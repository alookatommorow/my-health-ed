<% @comments.each do |comment| %>
	<div class="text-msg"><%= comment.body %></div>
<% end %>
<%= simple_form_for [@story, @comment] do |f| %>
  <%= f.input :body %>
  <%= f.button :submit %>
<% end %>